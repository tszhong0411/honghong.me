import { getCount } from './get-count'
import { getTotalCount } from './get-total-count'
import { patchCount } from './patch-count'

export const likesRouter = {
  getTotalCount,
  getCount,
  patchCount
}
