---
title: Form
description: Building forms with React Hook Form and Zod.
doc: https://react-hook-form.com
---

<ComponentPreview name='form/form' />

## Installation

```bash
pnpm dlx shadcn@latest add "https://docs.honghong.me/r/form"
```

## Usage

```tsx
import {
  Form,
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
  FormMessage
} from '@/components/ui/form'
```

```tsx
<Form {...form}>
  <form onSubmit={form.handleSubmit(onSubmit)}>
    <FormField
      control={form.control}
      name='...'
      render={({ field }) => (
        <FormItem>
          <FormLabel />
          <FormControl />
          <FormDescription />
          <FormMessage />
        </FormItem>
      )}
    />
  </form>
</Form>
```
